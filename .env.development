# Loaded automatically for `npm run dev` (Next.js). Not used on Vercel — see vercel.json `env` there.
# Phone dedupe bypass: preview/production safety is enforced in code (see application-phone-dedupe-bypass.ts).
APPLICATION_PHONE_DEDUPE_BYPASS_E164="+16506952683"
APPLICATION_PHONE_DEDUPE_BYPASS_REQUIRE_PREVIEW="true"
