# PostgreSQL — matches docker-compose.yml (postgres/postgres, db havyn)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/havyn?schema=public"

# Dev auth stub — `npm run db:setup` seeds the DB and writes these into .env.local automatically
DEV_ORGANIZATION_ID=""
DEV_USER_ID=""

# AI suggestions stub (optional)
ENABLE_AI_SUGGESTIONS="false"
